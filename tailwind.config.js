/** Root config for the Tailwind CLI — the real definitions live in assets/js/ so
 *  the browser (Play CDN) and the build step never drift apart. */
module.exports = require('./assets/js/tailwind.config.js');
